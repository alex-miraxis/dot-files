#!/bin/bash

# archy:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/archy/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  ARCHY_THEME_SKIP_BACKGROUND=1 archy-theme-set "$(cat $THEME_NAME_PATH)"
fi
