#!/bin/bash

# archy:summary=Launch a TUI or focus an existing terminal window for it
# archy:args=<command> [args...]

APP_ID="org.archy.$(basename "$1")"
LAUNCH_COMMAND="archy-launch-tui $@"

exec archy-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
