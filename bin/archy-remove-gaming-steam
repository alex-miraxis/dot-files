#!/bin/bash

# archy:summary=Remove Steam and all of its game libraries, configs, and caches.
# archy:requires-sudo=true

set -e

archy-pkg-drop steam

rm -rf \
  "$HOME/.steam" \
  "$HOME/.local/share/Steam" \
  "$HOME/.config/steam" \
  "$HOME/.cache/steam"

echo ""
echo "Steam and its data have been removed."
