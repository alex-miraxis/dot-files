#!/bin/bash

# archy:summary=Launch a floating terminal with the Arch presentation wrapper
# archy:args=<command>

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.archy.terminal --title=Arch -e bash -c "archy-show-logo; $cmd; if (( \$? != 130 )); then archy-show-done; fi"
