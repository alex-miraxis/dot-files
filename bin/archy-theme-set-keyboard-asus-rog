#!/bin/bash

# archy:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# archy:hidden=true

ASUSCTL_THEME=~/.config/archy/current/theme/keyboard.rgb

if archy-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
