#!/bin/bash

# archy:summary=Launch the Arch screensaver in the default terminal on the system with the correct font configuration.

if ! command -v tte &>/dev/null; then
  exit 1
fi

# Exit early if screensave is already running
pgrep -f org.archy.screensaver && exit 0

# Allow screensaver to be turned off but also force started
if archy-toggle-enabled screensaver-off && [[ $1 != "force" ]]; then
  exit 1
fi

# Silently quit Walker on overlay
walker -q

focused=$(archy-hyprland-monitor-focused)
terminal=$(xdg-terminal-exec --print-id)

hypr_focus_monitor() {
  hyprctl dispatch "hl.dsp.focus({ monitor = \"$1\" })" >/dev/null 2>&1 || hyprctl dispatch focusmonitor "$1" >/dev/null
}

hypr_exec() {
  local command="$1"

  hyprctl dispatch "hl.dsp.exec_cmd([[$command]])" >/dev/null 2>&1 || hyprctl dispatch exec -- bash -lc "$command" >/dev/null
}

for m in $(hyprctl monitors -j | jq -r '.[] | .name'); do
  hypr_focus_monitor "$m"

  case $terminal in
  *Alacritty*)
    hypr_exec "alacritty --class=org.archy.screensaver --config-file ~/.local/share/archy/default/alacritty/screensaver.toml -e archy-screensaver"
    ;;
  *ghostty*)
    hypr_exec "ghostty --class=org.archy.screensaver --config-file=~/.local/share/archy/default/ghostty/screensaver --font-size=18 -e archy-screensaver"
    ;;
  *foot*)
    hypr_exec "foot --app-id=org.archy.screensaver --config=\"$ARCHY_PATH/default/foot/screensaver.ini\" -e archy-screensaver"
    ;;
  *kitty*)
    hypr_exec "kitty --class=org.archy.screensaver --override font_size=18 --override window_padding_width=0 -e archy-screensaver"
    ;;
  *)
    notify-send -u low "✋  Screensaver only runs in Alacritty, Foot, Ghostty, or Kitty"
    ;;
  esac
done

hypr_focus_monitor "$focused"
