#!/bin/bash

# archy:summary=Toggle Waybar visibility
# archy:examples=archy toggle waybar

archy-toggle waybar-off

if pgrep -x waybar >/dev/null; then
  pkill -x waybar
else
  uwsm-app -- waybar >/dev/null 2>&1 &
fi
