#!/bin/bash

# archy:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# archy:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(archy-hyprland-monitor-focused)" "$@"
