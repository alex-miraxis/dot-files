#!/bin/bash

# archy:summary=Launch a TUI command in the default terminal with Arch styling
# archy:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.archy.$(basename $1) -e "$1" "${@:2}"
