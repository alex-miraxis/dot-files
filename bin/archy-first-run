#!/bin/bash

# archy:summary=Finish the installation of Arch with items that can only be done after logging in.
# archy:requires-sudo=true

set -e

FIRST_RUN_MODE=~/.local/state/archy/first-run.mode

if [[ -f $FIRST_RUN_MODE ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$ARCHY_PATH/install/first-run/battery-monitor.sh"
  bash "$ARCHY_PATH/install/first-run/recover-internal-monitor.sh"
  bash "$ARCHY_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$ARCHY_PATH/install/first-run/firewall.sh"
  bash "$ARCHY_PATH/install/first-run/dns-resolver.sh"
  bash "$ARCHY_PATH/install/first-run/gnome-theme.sh"
  bash "$ARCHY_PATH/install/first-run/gdk-scale.sh"
  bash "$ARCHY_PATH/install/first-run/swayosd.sh"
  bash "$ARCHY_PATH/install/first-run/gtk-primary-paste.sh"
  bash "$ARCHY_PATH/install/first-run/elephant.sh"
  archy-hook-install post-update "$ARCHY_PATH/install/first-run/install-voxtype.hook"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$ARCHY_PATH/install/first-run/welcome.sh"
  bash "$ARCHY_PATH/install/first-run/wifi.sh"
fi
