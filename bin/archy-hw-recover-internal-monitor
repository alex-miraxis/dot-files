#!/bin/bash

# archy:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/archy/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! archy-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
