#!/bin/bash

# archy:summary=Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Arch defaults and restart the service.

archy-refresh-config swayosd/config.toml
archy-refresh-config swayosd/style.css
archy-restart-swayosd
